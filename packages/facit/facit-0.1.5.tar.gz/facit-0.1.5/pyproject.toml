[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "facit"
dynamic = ["version"]
description = ''
readme = "README.md"
requires-python = ">=3.7"
license = "MIT"
keywords = []
authors = [
  { name = "Olle Vidner", email = "olle@vidner.se" },
  { name = "Camilla Wehlin" },
  { name = "Anton Wiberg" },
]
maintainers = [{ name = "Olle Vidner", email = "olle@vidner.se" }]
dependencies = []

[project.urls]
Homepage = "https://github.com/ovidner/pangolin"

[project.entry-points.pyinstaller40]
hook-dirs = "pangolin._pyinstaller_hooks:get_hook_dirs"

[tool.hatch.version]
source = "vcs"

[tool.hatch.build]
exclude = ["conda-recipe"]
