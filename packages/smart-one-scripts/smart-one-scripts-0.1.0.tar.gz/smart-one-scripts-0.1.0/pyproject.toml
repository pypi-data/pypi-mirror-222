[tool.poetry]
name = "smart-one-scripts"
version = "0.1.0"
description = ""
authors = ["Breno Santos"]
license = "MIT"
readme = "README.md"
packages = [{include = "smart_one_scripts"}]

[tool.poetry.dependencies]
python = ">=3.9"
pandas = "==2.0.0"
numpy = "==1.25.1"
unidecode = "1.3.6"
openpyxl = "==3.1.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
