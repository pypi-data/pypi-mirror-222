[tool.poetry]
name = "simple-regex-generator"
version = "0.1.0"
description = "This module is to generate simple regex pattern from string"
authors = ["Tomoya Wada <tomoyawada@gmail.com>"]
readme = "README.md"
packages = [{include = "simple_regex_generator"}]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
