[build-system]
requires = ["setuptools", "wheel", "setuptools-rust", "pymongo"]

[project]
name = "notmongo"
dependencies = ["pymongo"]
dynamic = ["version"]

[tool.setuptools.dynamic]
version = { attr = "notmongo_meta.__version__" }

[tool.black]

[tool.isort]
profile = "black"
