[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "doctorr"
version = "0.0.1"
dependencies = [
	'pyyaml',
	'jinja2',
	'watchdog'
]

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 88
