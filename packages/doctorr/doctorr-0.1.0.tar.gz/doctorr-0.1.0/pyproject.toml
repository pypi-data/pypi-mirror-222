[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "doctorr"
version = "0.1.0"
dependencies = [
	'pyyaml',
	'jinja2',
	'watchdog'
]
description = "Next level flexible autodocumentation tooling for python pros."
authors = [
    { name = "Rémi CONNESSON" }
]
readme = "README.md"

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 88
