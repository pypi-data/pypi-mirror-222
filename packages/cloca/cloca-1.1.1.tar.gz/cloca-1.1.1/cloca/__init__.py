from .cloca import Cloca

# Create a singleton instance of Cloca
cloca = Cloca()

# Define aliases for convenience
delayed = cloca.delayed
increase = cloca.increase
now = cloca.now
reset = cloca.reset