[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]

[project]
name = "Region_RNA"
version = "0.0.1"
description = "A Python package to fetch and print Dataframe for Region RNA data."
readme = "README.md"
authors = [
    {name = "Hossam Ibrahim", email = "hossam.ibrahim@hlag.com"},
]
license = { file = "LICENSE" }
dependencies = [
    "requests",
    "pandas",
]
