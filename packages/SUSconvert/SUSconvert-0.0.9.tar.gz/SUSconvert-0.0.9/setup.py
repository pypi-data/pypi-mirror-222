from setuptools import setup, find_packages

setup(
    name='SUSconvert',
    version='0.0.9',
    author='Francisco Ibarra',
    description='Increible librería para convertir tu texto en SUS..',
    packages=find_packages(),
    install_requires=[
    ],
    classifiers=[
        
    ],
)
