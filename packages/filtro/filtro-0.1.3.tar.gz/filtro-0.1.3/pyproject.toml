[tool.poetry]
name = "filtro"
version = "0.1.3"
description = "The official filtro python package."
authors = ["Andrea Pinto <andrea@filtro.ai>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.31.0"
pydantic = "^1.10.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
