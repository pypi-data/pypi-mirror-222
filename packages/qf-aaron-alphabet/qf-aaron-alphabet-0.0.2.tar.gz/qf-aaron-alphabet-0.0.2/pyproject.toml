[build-system]
requires = [
  "setuptools >= 40.9.0",
  "wheel",
]
build-backend = "setuptools.build_meta"

[project]
name = "qf-aaron-alphabet"
description = "quickly find searchterm"
version = "0.0.2"
authors = [
    { name = "Aaron Alphabet", email = "aaron.alphabet@posteo.org" }
]
requires-python = ">=3.11"
dependencies = [
]




