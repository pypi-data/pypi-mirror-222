[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ssd1306_plus"
version = "0.0.5"
authors = [
  { name="Code Cadets", email="codecadets@cgs.act.edu.au" }
]
description = "Augments the SSD1306_I2C MicroPython library with high-level functions"
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: Implementation :: MicroPython",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
